/** @type {import('next').NextConfig} */
module.exports = {
	reactStrictMode: true,
	images: {
		domains: ['api.github.com', 'avatars.githubusercontent.com', 'raw.githubusercontent.com', 'upload.wikimedia.org', 'logodix.com', 'assets.vercel.com'],
	}
}
